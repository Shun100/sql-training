# 集合

1. 和集合(UNION)
2. 差集合(EXCEPT)
3. 積集合(INTERSECT)

## 1. 和集合
- UNION: 2つのテーブルの和集合を取る(行を連結) 重複は消す
- UNION ALL: 重複は残す

## 2. 差集合
- EXCPET: 左のテーブルから右のテーブルを引いた差集合を取る 重複は消す
- EXCEPT ALL: 重複は残す

## 3. 積集合
- INTERSECT: 2つのテーブルの共通するデータを返す 重複は消す
